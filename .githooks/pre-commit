#!/usr/bin/env bash
# Runs scripts/check before every commit.
# Activate once per clone:  git config core.hooksPath .githooks
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"
exec scripts/check
